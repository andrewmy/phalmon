sudo: required
services:
  - docker

before_install:
  - docker version
  - docker-compose version
  - docker-compose up --build -d
  - docker ps -a

script:
  - docker-compose exec php_app codecept run

after_script:
  - docker-compose down
